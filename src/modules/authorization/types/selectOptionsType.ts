export interface ToolsGroupeItemType {
  label: string;
  value: string;
  imgIcons: string;
}

export interface GroupOptionsType {
  label: string;
  hexColor: string;
  options: ToolsGroupeItemType[];
}